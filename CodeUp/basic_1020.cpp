#include <iostream>
using namespace std;

int main(int argc, const char * argv[]) {
    // insert code here...
    int front, back;
    scanf("%d-%d", &front, &back);
    printf("%06d%07d", front, back);
    return 0;
}
